# Server
PORT=3000
NODE_ENV=development

# CORS — the frontend dev server origin (Vite default)
CORS_ORIGIN=http://localhost:5173

# Session/auth (see docs/BACKEND_REQUIREMENTS.md Epic 1 + Story: Session/auth security hardening
# in the frontend repo) — placeholders until the auth mechanism is implemented
SESSION_SECRET=change-me

# Database — placeholder until Epic 7 "Database schema & migrations" is scoped
DATABASE_URL=
